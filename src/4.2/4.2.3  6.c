//注意:代码模板中的代码将会被复制到任何新创建的文件中，编辑代码模板中的代码，让他帮你自动增加固定代码吧
#include <stdio.h> 
int main()
{
    float qt;
    double n,mm;
    int m;
    mm=3e-23;
    scanf("%f",&qt);
    m=qt*950;
    n=m/mm;
    printf("%f",n);
    return 0;
}